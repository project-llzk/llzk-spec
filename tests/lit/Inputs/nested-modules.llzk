module attributes { llzk.lang } {
  module @Bar {
    struct.def @Foo {
      struct.member @out : index
      function.def @compute() -> !struct.type<@Bar::@Foo<[]>> attributes {function.allow_non_native_field_ops, function.allow_witness} {
        %self = struct.new : <@Bar::@Foo<[]>>
        function.return %self : !struct.type<@Bar::@Foo<[]>>
      }
      function.def @constrain(%arg0: !struct.type<@Bar::@Foo<[]>>) attributes {function.allow_constraint, function.allow_non_native_field_ops} {
        %c0 = arith.constant 0 : index
        %c1 = arith.constant 1 : index
        %c2 = arith.constant 2 : index
        scf.for %i = %c0 to %c2 step %c1 {
          scf.yield
        } {loop_label = "same"}
        function.return
      }
    }
  }

  module @Baz {
    struct.def @Foo {
      struct.member @out : index
      function.def @compute() -> !struct.type<@Baz::@Foo<[]>> attributes {function.allow_non_native_field_ops, function.allow_witness} {
        %self = struct.new : <@Baz::@Foo<[]>>
        function.return %self : !struct.type<@Baz::@Foo<[]>>
      }
      function.def @constrain(%arg0: !struct.type<@Baz::@Foo<[]>>) attributes {function.allow_constraint, function.allow_non_native_field_ops} {
        %c0 = arith.constant 0 : index
        %c1 = arith.constant 1 : index
        %c2 = arith.constant 2 : index
        scf.for %i = %c0 to %c2 step %c1 {
          scf.yield
        } {loop_label = "same"}
        function.return
      }
    }
  }
}
